(ns codex.core
  (:gen-class))

(comment
  (def nums (range 1 18))

  ;; Getting practice slurp

  (def another-nums (range 1 4))
  (mod 4 3))
  (defn -main
    "I don't do a whole lot ... yet."
    [& args]
    (println "Hello, World!"))
  